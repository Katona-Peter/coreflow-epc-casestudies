import os

# Copy this file to env.py and update with your local development settings
# This file is for local development only - do not commit env.py to git

os.environ.setdefault(
    "DATABASE_URL", "your-database-url-here")
os.environ.setdefault("SECRET_KEY", "your-secret-key-here")
# Set DEBUG to True only for local development
os.environ.setdefault("DEBUG", "False")  # Change to "True" for local development only